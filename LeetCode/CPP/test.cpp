#include <bits/stdc++.h>
int main(int argc, char const *argv[])
{
    std::vector<int> a(5);
    for (int i = 0; i < 5; i++)
    {
        std::cout << a[i] << " ";
    }

    std::cout << "Hello";
    return 0;
}
